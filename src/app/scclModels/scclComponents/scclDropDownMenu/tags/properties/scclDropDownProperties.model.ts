export interface IScclDropDownProperties {
    inDuration?: number,
    outDuration?: number,
    constrainWidth?: boolean,
    gutter?: number,
    belowOrigin?: boolean,
    alignment?: string, 
    stopPropagation?: boolean
}